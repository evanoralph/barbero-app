# Shared sub-flow: relaunch and make sure we land on the login screen.
#
# Do NOT use `launchApp: { clearState: true }` on dev-client builds — it wipes
# the stored bundler URL and drops the device into the Expo launcher screen.
#
# Do NOT openLink to login while a session may exist — on Android that remounts
# the RN activity (endless splash / looks like a crash).
#
# Sign out sits just above the tab bar on small Android viewports. Prefer
# id: sign-out-button with a low visibilityPercentage — text + 80% visibility
# + centerElement fails even when the red button is already on screen.
appId: com.beruapp.ai
---
- launchApp:
    clearState: false
- waitForAnimationToEnd:
    timeout: 6000

- extendedWaitUntil:
    visible:
      text: "Dev quick fill|Book now|Quick actions|View public profile|Saved providers|Sign out"
    timeout: 45000

# Customer home → Account → Sign out
- runFlow:
    when:
      visible: "Book now"
    commands:
      - tapOn:
          id: "tab-account"
          optional: true
      - tapOn:
          text: ".*Account, tab.*"
          optional: true
      - extendedWaitUntil:
          visible: "Saved providers|Sign out|Settings"
          timeout: 15000
      - scrollUntilVisible:
          element:
            id: "sign-out-button"
          direction: DOWN
          centerElement: false
          visibilityPercentage: 40
          speed: 60
          timeout: 25000
      - tapOn:
          id: "sign-out-button"

# Provider dashboard → Profile → Sign out
- runFlow:
    when:
      visible: "Quick actions"
    commands:
      - tapOn:
          id: "tab-profile"
          optional: true
      - tapOn:
          text: ".*Profile, tab.*"
          optional: true
      - extendedWaitUntil:
          visible: "View public profile|Manage services|Sign out"
          timeout: 15000
      - scrollUntilVisible:
          element:
            id: "sign-out-button"
          direction: DOWN
          centerElement: false
          visibilityPercentage: 40
          speed: 60
          timeout: 25000
      - tapOn:
          id: "sign-out-button"

# Already on provider Profile (e.g. last tab after relaunch)
- runFlow:
    when:
      visible: "View public profile"
    commands:
      - scrollUntilVisible:
          element:
            id: "sign-out-button"
          direction: DOWN
          centerElement: false
          visibilityPercentage: 40
          speed: 60
          timeout: 25000
      - tapOn:
          id: "sign-out-button"

# Already on customer Account
- runFlow:
    when:
      visible: "Saved providers"
    commands:
      - scrollUntilVisible:
          element:
            id: "sign-out-button"
          direction: DOWN
          centerElement: false
          visibilityPercentage: 40
          speed: 60
          timeout: 20000
      - tapOn:
          id: "sign-out-button"

- extendedWaitUntil:
    visible: "Dev quick fill"
    timeout: 20000
